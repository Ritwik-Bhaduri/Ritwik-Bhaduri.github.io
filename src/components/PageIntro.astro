---
interface Props {
  title: string;
  intro?: string;
}

const { title, intro } = Astro.props;
---

<section class="container page-intro-outer">
  <div class="page-intro">
    <h1>{title}</h1>
    {intro && <p>{intro}</p>}
    <slot name="actions" />
  </div>
</section>
